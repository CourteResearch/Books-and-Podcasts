google-generativeai
reportlab
Flask
Flask-CORS # Add CORS support
# Flask-SocketIO removed
python-dotenv # Good practice for managing API keys via .env
gunicorn # WSGI server for production/hosting
requests # Added for podcast agent logic (potential future use)


python -c "try: from google.cloud import aiplatform; print('Success: google.cloud.aiplatform imported') except ImportError as e: print(f'Failed to import google.cloud.aiplatform: {e}')"
